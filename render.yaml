services:
  - type: web
    name: warehouse-route-system
    env: python
    buildCommand: "./build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.1
    disk:
      name: persistent-data
      mountPath: /var/data
      sizeGB: 1
